# --- Dependencies (ไลบรารีต่างๆ ที่ติดตั้งผ่าน npm/yarn/pnpm) ---
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# --- Testing (ผลลัพธ์การเทส) ---
/coverage

# --- Next.js Build Output (ไฟล์ที่ Next.js สร้างขึ้นตอนรัน ห้ามอัปโหลด) ---
/.next/
/out/

# --- Production Build (เผื่อมีการ Build แบบ Custom) ---
/build
dist

# --- TypeScript Specific (สำคัญสำหรับโปรเจกต์ TypeScript) ---
# ไฟล์ Cache ของ TS
*.tsbuildinfo
# ไฟล์ Type Definition ที่ Next.js สร้างให้อัตโนมัติ (ห้ามแก้เอง และไม่ต้องอัปโหลด)
next-env.d.ts

# --- Debug Logs (ไฟล์แจ้งเตือนข้อผิดพลาด) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Environment Variables (ความลับ! ห้ามอัปโหลดเด็ดขาด) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# อนุญาตให้อัปโหลดไฟล์ตัวอย่าง (เพื่อให้คนอื่นรู้ว่าต้องตั้งค่าตัวแปรอะไรบ้าง)
!.env.example

# --- Vercel (ถ้าใช้ Vercel ในการ Deploy) ---
.vercel

# --- ESLint (Cache การตรวจโค้ด) ---
.eslintcache

# --- Editor Directories (การตั้งค่าส่วนตัวของ Editor) ---
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# --- OS System Files (ไฟล์ขยะของระบบปฏิบัติการ) ---
.DS_Store
Thumbs.db